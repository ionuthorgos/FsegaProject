import React, {PropTypes} from 'react';

const FormContact = ({variabilaOptional}) => {
  return (

    <div >
    </div>
  );
};



FormContact.propTypes = {
  course: PropTypes.array.isRequired
};

export default FormContact;
